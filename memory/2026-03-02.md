# 2026-03-02 Memory

## Ollama 修复
- 问题：local_heartbeat.py 调用 ollama 失败，报 "No such file or directory: 'ollama'"
- 原因：cron 环境 PATH 不含 ollama 路径
- 修复：使用完整路径 `/opt/homebrew/bin/ollama`
- 状态：修复成功，但 qwen2.5:3b 模型太小，AI 判断不准确

## Qwen3.5 模型
- Ollama 版本太旧，无法下载 qwen3.5:27b
- 用户已升级 Ollama
- 待下载：ollama pull qwen3.5:27b

## Mission Control 项目
- 仓库：https://github.com/crshdn/mission-control
- 位置：~/Documents/mission-control
- 状态：npm install 被 SIGKILL（内存不足），安装失败
- 待解决：重新安装

## 今日定时任务
| 时间 | 任务 | 状态 |
|------|------|------|
| 08:00 | GitHub AI 趋势 | ✅ |
| 08:30 | QVeris AI 趋势 | ✅ |
| 09:00 | AI 日报 | ✅ |
| 09:30 | 4A 广告 | ✅ |
| 10:00 | TikTok 泰区 | ✅ |
| 18:00 | OpenClaw 话题 | ✅ |

## Simmer 监控
- 今日多次扫描，未发现 >65% 或 <35% 的高确信度机会
- 持仓状态：4个，PnL -$9.58

## 遗留问题
- Mission Control 安装失败，需要用低资源方式重装
- Ollama 模型待升级到 qwen3.5:27b

## OpenClaw 升级
- 已从 2026.2.25 升级到 **2026.3.1**（npm install -g openclaw@latest）
- 已执行 gateway restart 以加载新版本

## OpenClaw 2026.3.1 升级后提示
- `openclaw gateway status` 提示 LaunchAgent 使用 nvm 的 node 路径，升级后可能不稳定
- 建议后续运行：`openclaw doctor --repair`

## ACP/Codex 状态
- 已安装 Codex CLI：`@openai/codex`（codex-cli 0.106.0）
- 尝试 `sessions_spawn(runtime="acp", agentId="codex")` 失败：提示缺少 ACP 后端
  - 错误：`ACP runtime backend is not configured. Install and enable the acpx runtime plugin.`
- 结论：需要安装并启用 `acpx` runtime plugin 才能让 OpenClaw 使用 ACP 调 Codex

## ACPX 启用尝试
- OpenClaw 已升级到 2026.3.1
- 已安装 acpx CLI：`npm install -g acpx@0.1.15`
- 已在 `~/.openclaw/openclaw.json` 的 plugins.entries 中启用 `acpx: { enabled: true }`
- 已重启 gateway 以加载插件
- 下一步：再次测试 `sessions_spawn(runtime="acp", agentId="codex")`

## ACP 已打通但输出不可见
- ACP 调 Codex 已能成功排队（spawn accepted）
- 当前受限：主会话无法读取 `agent:codex:acp:*` 的 sessions_history（tools.sessions.visibility 非 all）
- 解决方案：用户可在当前聊天用 `/acp spawn codex --mode oneshot --thread off` 让输出直接回到对话；或放开 tools.sessions.visibility=all

## tools.sessions.visibility
- 已按用户确认将 `tools.sessions.visibility` 设置为 `all`（写入 ~/.openclaw/openclaw.json）
- 已重启 gateway 生效
- 目的：允许主会话读取 ACP/Codex 子会话 history

## tools.agentToAgent.enabled
- 为解决 `sessions_history` 仍 forbidden（agent-to-agent history disabled）
- 已设置 `tools.agentToAgent.enabled=true`
- 已重启 gateway 生效

## ACP 关键开关 & 当前状态
- 已按用户要求打开跨会话可见性：
  - `tools.sessions.visibility=all`
  - `tools.agentToAgent.enabled=true`
- acpx 插件已启用并在 gateway 日志显示 ready
- 发现 ACP 仍未执行：gateway 日志报 `ACP dispatch is disabled by policy (acp.dispatch.enabled=false)`
- 已随后写入 `acp.enabled=true` 与 `acp.dispatch.enabled=true` 并重启 gateway

## 市场调研：OpenClaw 哪类 skill 吃香
- 结论倾向：工作流自动化、通用集成（Feishu/Slack/Telegram/Sheets/GitHub）、浏览器自动化（做得稳很值钱）、可靠性/运维守护（watchdog、自愈、告警、降级）、成本优化/预算控制。

## 调研：创意类 skill
- 创意类吃香方向：广告创意生成（brief/脚本/分镜）、短视频脚本→分镜→提示词→成片流水线、品牌一致性/风格模板（Brand Kit）、素材拆解入库与复用、参数化提示词产品化。
- 建议结合现有生态（Telegram 话题 + Seedance/Jimeng + 工作流）做“广告创意拆解 + 分镜 + 提示词生成器（带品牌模板）”

## 创意类 Skill 方向确认
- 用户选择“广告创意生成”方向，并希望结合 Seedance 提示词输出
- 建议 MVP 输出：creative brief、脚本(6/15/30s)、分镜表、逐镜 Seedance prompts（含 style/camera/lighting/negative/brand constraints）

## 需求定义：泰国 TikTok 高转化 Seedance 提示词
- 需求：用户每次提供「1 张产品图 + 中文产品名」，需输出适用于 Seedance 的完整提示词包，围绕产品，不跑偏；可输出多风格脚本。
- 类目：咖啡用品/家居/露营/工具/宠物产品。
- 建议默认输出 3 套风格：UGC 测评、对比挑战、高级感广告片；每套含脚本+分镜+逐镜 Seedance prompts+negative。

## Seedance 提示词交付（样例：酸枣木碗）
- 已根据用户提供的产品图与名称“酸枣木碗”输出 3 套风格：UGC 测评、对比挑战、高级感广告片。
- 每套包含：脚本+6-7镜分镜+逐镜 Seedance prompts（强绑定产品出镜/特写/动作）+通用 negative。

## 泰国TikTok文案参数（酸枣木碗）
- 卖点确认：质感木纹、拍照出片
- 折扣价：142.5 THB
- 已补充：三套风格的泰语 Hook/CTA，并建议 A(UGC)优先转化

## Seedance 输出偏好补充
- 用户要求：露脸（UGC），且泰语文案与字幕需要写进 Seedance 提示词中（而非后期单独加）。
- 已给出：UGC 露脸版 6 镜头结构，包含泰语口播脚本 + 逐句字幕 + 字幕样式规范 + 每镜 prompt。

## Seedance 口播+字幕（泰语）最终偏好
- 用户确认：泰国女生露脸；希望 Seedance 生成时同时包含泰语配音与泰语字幕（写进提示词）
- 已交付：15s/6镜头版本，含 GLOBAL 约束 + 每镜 VO+字幕文本 + prompt

## 脚本产出记录（需长期复用）
- 产品：酸枣木碗（卖点：质感木纹、拍照出片；折后价 142.5 THB）
- 输出模板：泰国 TikTok 15s / 9:16 / 泰国女生露脸 UGC
- 要求：Seedance 生成时同时包含泰语配音(VO) + 泰语字幕（字幕规范：Thai sans-serif，白字黑描边，下方安全区，清晰可读）
- 内容结构：6 镜头（Hook 近景木纹 + 木纹特写 + 厚边手感 + 装水果出片 + 价格锚点 + CTA hero 定格）
- VO（Thai）：
  1) “ทุกคนดูนี่… ชามไม้ลายสวยมาก!”
  2) “ถ่ายรูปขึ้นกล้องสุดๆ โต๊ะดูแพงขึ้นเลย”
  3) “ขอบหนา จับถนัด งานไม้แท้”
  4) “ใส่ผลไม้ก็สวย ถ่ายรูปได้ทุกมื้อ”
  5) “ตอนนี้ลดแล้ว เหลือแค่ 142.5 บาทเอง”
  6) “ใครชอบสไตล์มินิมอล กดสั่งเลย!”
- Subtitle（Thai）：
  S1: “ชามไม้ลายสวยมาก!”
  S2: “ถ่ายรูปขึ้นกล้องสุดๆ”
  S3: “ขอบหนา • งานไม้แท้”
  S4: “ใส่ผลไม้ก็สวย”
  S5: “ลดเหลือ 142.5 บาท”
  S6: “กดสั่งเลย”
- GLOBAL PROMPT：强调单一 hero 产品（仅 1 个木碗）、每镜必须出现木碗/木纹特写、无 logo/watermark、加入防泰文乱码/错数字的 negative。

## Skill 发布意向
- 用户希望将当前“泰国TikTok Seedance 提示词生成”skill 上传到 GitHub。
- 待确认：目标 GitHub 仓库 URL/owner、skill 名称、以及形态（纯模板 vs 带脚本生成器）。

## GitHub 发布完成
- 已创建并推送仓库：https://github.com/19960705/th-tiktok-seedance-generator （private）
- 内容：skill + 脚本生成器 `generate_seedance_prompt.py`，可生成泰国 TikTok Seedance 提示词 Markdown（含泰语 VO+字幕约束）
- 本地路径：/Users/mac/.openclaw/workspace/skills/th-tiktok-seedance-generator

## Skill 升级：可选图片分析
- 已升级 th-tiktok-seedance-generator：新增 `--analyze-image`（Pillow）提取主色调/亮度用于丰富 GLOBAL PROMPT，更贴合产品图。
- 新增 requirements.txt（pillow），脚本未安装依赖时会降级并提示。
- 已提交并 push 至 GitHub 仓库 main。

## 云端识别选择
- 用户选择：使用火山（Volcengine/Ark）作为云端视觉识别提供方，用于从产品图提取材质/颜色/形状等并写入 Seedance prompts。
- 待用户提供：Ark 视觉模型的 model id/名称；确认复用现有 volcengine 配置或单独新增 vision 配置。

## Volcengine 云端视觉识别已接入（Doubao-Seed-1.8）
- 已在 th-tiktok-seedance-generator 中新增云端识别开关：`--vision volcengine`
- 默认模型：`Doubao-Seed-1.8`
- 默认 endpoint：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`
- key：读取 `VOLCENGINE_API_KEY` 或 `ARK_API_KEY`；未设置则自动降级跳过
- 识别输出用于丰富 GLOBAL PROMPT：material/color/shape/category/scene/details 等
- 已提交并推送到 GitHub main
- ⚠️ 需要后续清理：误提交了 `__pycache__/*.pyc`（应添加 .gitignore 并删除追踪文件）

## 使用协议（对话内生成）
- 用户确认将优先在对话中使用：发产品图+中文名（可选卖点/价格）并说“生成 Seedance 脚本（泰国女生露脸+泰语配音+字幕）”，助手直接输出完整提示词包。
- 同时保留本地脚本方式作为备选。

## Flomo 记录需求
- 用户询问 flomo，并希望助手将一些“有趣的想法”记录到 flomo。
- 待确认写入方式：flomo 收集邮箱（最简）或 webhook/接口 URL（更灵活，可自动标签/去重/批量）。
- 待确认内容范围与格式：产品洞察/广告创意/选品/自动化/交易复盘等；一句话+标签 vs 结构化卡片。

## Flomo Webhook 已接通
- 用户提供 flomo webhook：https://flomoapp.com/iwh/NzAzMjQ/f36bb79a063916e80f44ce4762d8a58a/
- 已成功 POST 写入测试卡片（返回：已记录），可用于后续自动记录产品洞察/广告创意/灵感/助手想法。
- 待用户确认自动记录触发规则（每次产出脚本自动记 vs 手动指令 vs 每日合集）。

## flomo/Obsidian 自动记录规则（已开始执行）
- 用户授权：自动保存
  - 报告类 → Obsidian
  - 洞察/脚本/创意/助手想法 → flomo
- flomo webhook 写入已成功（已写入模板洞察+酸枣木碗脚本两条）
- 建议默认节流：脚本/洞察各写一条精简卡片，避免刷屏

## flomo 标签偏好
- 用户确认：flomo 标签使用「纯中文」体系
- 默认标签：#产品洞察 #广告创意 #脚本 #随机灵感 #keonho + #泰国TikTok #Seedance + 类目标签

## flomo/Obsidian 记录规则更新（用户最终确认）
- 不做 flomo↔Obsidian 链接。
- 脚本类：直接在对话中发送给用户 + 同步保存到 Obsidian（不再写入 flomo）。
- 洞察类：只写入 flomo（如探险/复盘后的可提升点、广告创意、随机灵感、助手想法）。
- 待确认：Obsidian 中脚本类保存目录（Scripts/Seedance vs Reports vs 指定路径）。

## Obsidian 脚本归档目录
- 用户选择脚本类保存目录：`Scripts/Seedance/`
- 已创建目录并将酸枣木碗完整版脚本移动至：
  - `Scripts/Seedance/Seedance-酸枣木碗-142.5THB-2026-03-02.md`

## Simmer 高确信度机会（2026-03-02 晚）
- Polymarket:
  - Solana Up/Down (9:00-9:15AM ET): 0.686
  - XRP Up/Down (9:00-9:15AM ET): 0.717
  - Ethereum Up/Down (9:05-9:10AM ET): 0.814
  - S&P 500 Opens Up/Down (Mar 2): 0.01

## 明日探险提案
- 向用户提出 3 个探险方向：
  A) 脚本工厂升级：一张图→3风格→可跑Seedance（ugc/对比/高级感）+ 字幕/数字稳定策略
  B) 火山云端识别实测对齐：Doubao-Seed-1.8 视觉 JSON schema 稳定化 + 写入每镜 prompt + 降级策略
  C) 自动沉淀系统：洞察/脚本/报告分类写入 flomo/Obsidian + 去重/摘要
- 建议默认优先 A（提升日产能力）

## 明日探险默认输出偏好
- 用户选择：每个产品默认输出 3 条脚本：UGC 2 条 + 对比挑战 1 条（选项3）。
