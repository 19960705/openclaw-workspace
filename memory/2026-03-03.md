
## 18:56 (Asia/Shanghai) — flomo webhook automation
- Stored webhook in `scripts/.env.flomo` (FLOMO_WEBHOOK_URL)
- Added scripts:
  - `scripts/flomo_push.sh` (send arbitrary note)
  - `scripts/flomo_daily_insights.sh` (tail today's memory and push)
- Verified sending: test note + daily insight both returned OK
- Cron on this host appears to hang when updating; switched to macOS LaunchAgent instead
- Created + loaded LaunchAgent: `~/Library/LaunchAgents/com.keonho.flomo-daily.plist`
  - Schedule: daily 19:10
  - RunAtLoad: true
  - Logs: `~/.openclaw/logs/flomo_daily.log` + `_err.log`
