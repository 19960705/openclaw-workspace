
## 14:06 (Asia/Shanghai) — Gateway healthcheck script created
- Created `scripts/gateway-healthcheck.sh`: checks gateway RPC probe, auto-restarts if unreachable
- Tested: currently healthy (exit 0)
- Usage: call before cron tasks that need gateway connectivity
- Logs to: `~/.openclaw/logs/gateway-healthcheck.log`
- Next: document usage or apply to existing cron scripts
