
## 23:45 - TuriX CUA / OpenClaw skill 安装进展
- 通过 ClawHub 安装成功：`turix-cua` → `/Users/mac/.openclaw/skills/turix-cua`（Skill frontmatter name: `turix-mac`），包含 `SKILL.md` 与 `scripts/run_turix.sh`。
- ClawHub CLI 登录已完成（whoami: `19960705`）。
- 尝试 clone `https://github.com/TurixAI/TuriX-CUA` 到 `/Users/mac/.openclaw/workspace/projects/TuriX-CUA` 多次，但 clone 过程被中断/残缺（出现 `.git/HEAD -> refs/heads/.invalid`，随后目录消失；最终未在 workspace 中找到 `TuriX-CUA` 目录）。
- 因仓库未确认存在，为避免误导，将 `run_turix.sh` 里的 `PROJECT_DIR` 恢复为占位符 `your_dir/TuriX-CUA`，待下次成功 clone 后再改为真实路径。
- 下一步（待办）：重新 clone（建议 `git clone --depth 1 ...` 并确认目录内容）；然后按 README 创建 conda env `turix_env` (py3.12) + `pip install -r requirements.txt`；最后手动授予 macOS Screen Recording/Accessibility/Safari Remote Automation 权限。
