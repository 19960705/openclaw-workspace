
## 23:47 - TuriX CUA clone 修复 + 依赖/权限前置条件
- 解决了之前 git clone 中断/残缺问题：改用 `git clone --filter=blob:none --depth 1` 成功拉取到 `/Users/mac/.openclaw/workspace/projects/TuriX-CUA`（commit: a0a76a6）。
- 已将 `/Users/mac/.openclaw/skills/turix-cua/scripts/run_turix.sh` 的 `PROJECT_DIR` 更新为真实路径：`/Users/mac/.openclaw/workspace/projects/TuriX-CUA`。
- 注意：当前 shell 中 `conda/pip` 命令不可用（用户终端报 `command not found`），但系统自带 `python3` 与 `python3 -m pip` 可用（Python 3.14.3）。后续需安装 Conda/Miniconda（或按仓库要求确保 Python 3.12 环境）才能运行 TuriX。
- 运行前必须手动授予 macOS 权限：Screen Recording + Accessibility（Terminal/VSCode/node/python3）以及 Safari Remote Automation。
