
## 00:35 - TuriX CUA 完整安装完成
- Miniconda3-latest（arm64）已通过官方脚本安装到：`/Users/mac/miniconda3`
- conda init 已添加到 `~/.zshrc` 与 `~/.bash_profile`
- turix_env（Python 3.12）已创建；requirements.txt 已用 `turix_env` 完整 pip install
- TuriX-CUA repo 已成功 clone（depth 1）到：`/Users/mac/.openclaw/workspace/projects/TuriX-CUA`（commit a0a76a6）
- ClawHub 上的 `turix-cua` skill 已安装到：`/Users/mac/.openclaw/skills/turix-cua`
- 已更新 skill 的 `scripts/run_turix.sh`：
  - `PROJECT_DIR="/Users/mac/.openclaw/workspace/projects/TuriX-CUA"`
  - `CONDA_PATH="/Users/mac/miniconda3/bin/conda"`
- 用户已手动授予 macOS 权限：Screen Recording / Accessibility（待确认）；Safari Remote Automation（待确认）
- 下一步：测试 `bash /Users/mac/.openclaw/skills/turix-cua/scripts/run_turix.sh "Open Safari and go to github.com"`

## 01:00 - TuriX 首次试跑失败原因（屏幕录制权限）
- 执行：`/Users/mac/.openclaw/skills/turix-cua/scripts/run_turix.sh "Open Safari and go to github.com"`
- 结果：preflight 报 Screen Recording 可能缺失，运行时明确提示 "Please enable screen recording permission for this script"，随后 `conda run ... examples/main.py` 退出。
- 需要在 macOS "屏幕录制" 权限中添加/勾选：运行 OpenClaw 的宿主进程（通常是 `node`）+ 当前终端 + conda env 的 python（`~/miniconda3/envs/turix_env/bin/python`），并在授权后**彻底退出并重开**相关 App 才会生效。

## 01:02 - Screen Recording 权限仍未生效（CGPreflight=False）
- 复测：`/Users/mac/miniconda3/envs/turix_env/bin/python -c ...CGPreflightScreenCaptureAccess()` 返回 `False`
- 直接运行 `run_turix.sh` 仍提示 "Please enable screen recording permission" 并退出。
- 推断：权限并未真正授权到当前实际执行进程（可能需要：彻底退出 Terminal/VSCode 后重开；或在 Screen Recording 中添加具体二进制：`/Users/mac/miniconda3/envs/turix_env/bin/python`、`/Users/mac/miniconda3/bin/python`、以及运行 OpenClaw 的 `node` 路径）。

## 01:50 - TuriX 改用阿里百炼（DashScope coding）OpenAI-compatible Vision
- 用户提供的 baseUrl：`https://coding.dashscope.aliyuncs.com/v1`，模型：`qwen3.5-plus`（text+image）与 `kimi-k2.5`（text+image）。
- 已将 `projects/TuriX-CUA/examples/config.json` 切换为 `provider: turix`（即 ChatOpenAI + openai_api_base）并使用 `qwen3.5-plus` 作为 brain/actor/planner/memory 的 model_name。
- 出于安全，API Key 不写入 config.json；运行时应通过环境变量 `API_KEY` 或 `OPENAI_API_KEY` 提供。
- 本地 Ollama vision 拉取（llama3.2-vision）因耗时大且 SIGTERM 中断，改走云端 vision。

## 02:06 - 成功：OpenClaw 侧调用 TuriX (qwen3.5-plus vision)
- 采用方案：停用 launchd gateway，改为在用户 Terminal 前台启动 gateway（继承 ScreenCapture 权限）。
- 运行成功：`run_turix.sh "Open Safari and go to github.com"` 完整执行 4 步：打开 Safari → 点击地址栏 → 输入 github.com → 回车 → 到达 GitHub 首页 → done。
- 视觉模型：DashScope OpenAI-compatible (`base_url=https://coding.dashscope.aliyuncs.com/v1`) + `model=qwen3.5-plus`。
- API Key：通过 macOS Keychain (`turix_dashscope_api_key`) 注入为环境变量 `API_KEY`（未落盘）。
- 注意：日志仍出现 `langchain_google_genai` deprecation warning（依赖引入但不影响本次执行）。

## 02:25 - 自动化：LaunchAgent 自动在 Terminal 前台启动 OpenClaw Gateway（继承屏幕录制权限）
- 创建脚本：`/Users/mac/.openclaw/workspace/scripts/openclaw_gateway_in_terminal.sh`
  - 逻辑：若 127.0.0.1:18789 已在监听则退出；否则用 `/opt/homebrew/bin/node ... openclaw ... gateway --port 18789` 启动。
- 创建 LaunchAgent：`~/Library/LaunchAgents/com.keonho.openclaw-gateway-terminal.plist`
  - 通过 osascript 激活 Terminal 并执行上述脚本
  - 日志：`~/.openclaw/logs/gateway_terminal_launcher*.log`
- 安装并启动：`launchctl bootstrap + kickstart`
- 目标：避免 launchd 运行的 gateway 丢失 TCC ScreenCapture 权限，保证 OpenClaw 可稳定调用 TuriX CUA。
## 03:25 — X 关键词书签扫描（TuriX）阶段性记录
- 用户授权：允许加书签 + 用搜索流（关键词：AIAGENT / OPENCLAW / CLAUDE CODE）。
- 已执行：将系统音量调低到 10%（osascript）。
- 已确认成功加书签（X 界面提示“已添加到你的书签”）：
  - Pedro E. Caparrós Torres (@Guelug)
  - https://x.com/Guelug/status/1896828676256641435
  - 理由：提到用 OpenClaw 做“money safe reasons/资金安全”相关实践（具体应用场景）。
- Obsidian 已建立可复核日志：AI/X书签扫描-2026-03-04.md，并已写入上述已确认书签。
- 新问题：TuriX 在“X 搜索流”多次出现启动后无进一步输出/疑似卡住（仅显示 Press Cmd+Shift+2 to force stop）。相关 session 被手动 kill（kind-daisy / good-sage / clear-valley）。
- 处置策略：临时改为优先从 Following 流扫关键词（已验证该路径可滚动），搜索流待排障。
## 03:34 — TuriX“卡住”误判原因 & Following 流扫描产出
- 现象：TuriX 多次启动后仅打印“Press Cmd+Shift+2 to force stop”且无新输出，误判为卡死。
- 真实原因：stdout/日志输出存在缓冲/延迟；从 .turix_tmp/temp_files 可见任务其实在后台继续执行并产出 record_info。
- Following 流 30s 扫描（不做书签）产出：
  - Claude Code：Min Choi，提到 Claude Code 的 /voice 功能（UI 仅显示 x.com 未给完整 URL）。
  - Perplexity Computer：AI Edge (@aiedge_) 对比 Perplexity Computer vs OpenClaw（含 setup/workflow/pro tips/connectors）。
- 后续策略：优先从 .turix_tmp/temp_files/records/*.txt 抽取可复核文本，再写入 Obsidian；避免因 stdout 静默就 kill。
## 03:36 — 修复 TuriX“看起来卡住”问题（stdout 缓冲）
- 修改：~/.openclaw/skills/turix-cua/scripts/run_turix.sh
  - export PYTHONUNBUFFERED=1
  - 运行命令改为：python -u examples/main.py
  - stdout/stderr tee 到 ~/.openclaw/logs/turix_YYYYmmdd_HHMMSS.log，便于排障与复核
- 目的：避免只看到“Press Cmd+Shift+2…”就误判卡死；即使调用侧不显示输出，也能在日志里看到进度。
## 03:48 — TuriX 输出“不断流”修复验收通过
- Smoke test：X Following/推荐流滚动 20 秒（不点赞/不转发/不关注/不回复/不书签）
- 结果：TuriX 连续输出 step/action，任务成功完成（多个 scroll_down + done）
- 运行日志：~/.openclaw/logs/turix_20260304_034801.log
## 11:09 — Durables (compaction)
- 用户确认：以后 Perplexity / Flowith / Grok / 即梦 等网页/桌面操作可以交给我执行；今晚先收工，明天继续。
- TuriX 稳定性修复：run_turix.sh 增加 PYTHONUNBUFFERED=1 + python -u + tee 到 ~/.openclaw/logs/turix_*.log；已通过 X Following 流滚动 20 秒 smoke test（log: ~/.openclaw/logs/turix_20260304_034801.log）。
- X 书签扫描记录：Obsidian 已建立并追加：AI/X书签扫描-2026-03-04.md；已确认成功书签一条 https://x.com/Guelug/status/1896828676256641435。
- 自由活动方向确认：A + C（固化 X 书签扫描工作流 + 【行动】1 分钟内容原型 3 版本）。
## 11:12 — Cron 最低保障模型（修复上下文窗口误配）
- 发现：Ollama qwen3.5:9b 实际 context length = 262144（ollama show），但 openclaw.json 中该模型 contextWindow 误设为 8192，导致需要 >=16000 context 的 cron 任务触发 FailoverError。
- 修复：将 ~/.openclaw/openclaw.json 中 ollama/qwen3.5:9b 的 contextWindow 调整为 262144。
- 说明：无需更换 primary/fallback；只修正模型元数据即可满足 cron 的最低上下文要求。
## 13:18 — Heartbeat 改为以 launchctl+log 为准（修复“没跑也打钩/输出一模一样”）
- 改动文件：scripts/local_heartbeat.py
- 变更：
  - 新增 TASK_RUNTIME 映射：task id -> LaunchAgent label + stdout/stderr log
  - Heartbeat 判定优先用 log mtime（是否今天更新）+ launchctl print（runs/exit 辅助）
  - tasks.json 的 status 仅作为 fallback，不再当“✅ 已完成”证据
  - 降低本地模型分析超时（20s）；若 AI 输出过短（❌/TIMEOUT/ERROR）则跳过 Telegram 推送，避免误报/刷屏
- 当前 heartbeat 样例：能正确识别 github/qveris/thailand-trend 已运行（依据 log 时间 08:00/08:30/10:00），并标出 twitter-digest/4A/simmer 逾期未运行。
## 13:22 — Heartbeat A 方案加强：无映射任务不再“误打钩”
- local_heartbeat.py：对未接入 LaunchAgent/log 映射的任务，不再显示 ✅（即便 tasks.json 写 completed），统一加注“tasks.json 可能陈旧”。
- 结果：heartbeat 输出更可信；真正跑过的任务会因 log mtime 显示 ✅。
## 13:30 — Heartbeat 全覆盖（新增 LaunchAgents + 映射）
- 新增脚本：
  - scripts/ai-twitter-digest.sh（暂未实现，写入 [NOT_IMPLEMENTED] 并 exit 1）
  - scripts/ad-visual-daily.sh（暂未实现，写入 [NOT_IMPLEMENTED] 并 exit 1）
  - scripts/simmer-opportunities-daily.sh（可运行：调用 simmer-check.py opportunities 并推送 Telegram topic:6）
- 新增 LaunchAgents：
  - ~/Library/LaunchAgents/com.keonho.ai-twitter-digest.plist（09:00）
  - ~/Library/LaunchAgents/com.keonho.ad-visual-daily.plist（09:30）
  - ~/Library/LaunchAgents/com.keonho.simmer-opportunities.plist（01:00）
- Heartbeat 映射更新：task-twitter-001 / task-ad-visual-001 / task-simmer-001 已接入 label+log。
- Heartbeat 规则：若 stderr tail 含 [NOT_IMPLEMENTED]/Traceback/ERROR，则标记为“运行失败/未实现”，避免误报 ✅。
## 13:35 — Thailand trend 报错已修正 + Heartbeat stderr 误判修复
- Thailand trend 真实状态：任务可用备份数据生成趋势文件；之前 stderr 的 selenium 缺失是旧日志（Mar 2）。
- 修复：heartbeat 仅在 stderr log 今日更新时才判定为失败；避免旧 Traceback 误判。
- 同时安装 selenium（/usr/bin/python3 --user），但 thailand_trend_monitor 实际走备份数据路径也可完成。
- Heartbeat 本地 AI 可能 TIMEOUT：已加“fallback 规则”（当 TIMEOUT 且无 ❌ 时判定 OK）；有 ❌ 则仍保持告警。
